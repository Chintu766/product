<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>
<dd><%= image_tag(@image.picture.url, size: "150x100") if @image.picture? %></dd>
<p>
  <strong>Picture:</strong>
  <%= @image.picture %>
</p>

<p>
  <strong>Article:</strong>
  <%= @image.article_id %>
</p>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
